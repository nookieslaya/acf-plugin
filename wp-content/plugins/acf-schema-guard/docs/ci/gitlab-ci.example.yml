acf_schema_guard_check:
  image: wordpress:cli
  script:
    - '# Bootstrap WordPress, its database, ACF, and this plugin before this command.'
    - wp acf-schema-guard baseline check acf-schema-baseline.json --fail-on-breaking
